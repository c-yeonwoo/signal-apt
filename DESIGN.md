# Signal APT — 디자인 시스템

> 신규 기능·화면 추가 시 **이 문서의 토큰·규칙을 먼저 따른다.** 새 색·폰트·라운드를 임의로 만들지 말 것.
> 모든 스타일은 `src/realty_signal/web/index.html` 단일 파일(인라인 + `<style>`)에 있음.

## 1. 브랜드
- **아이덴티티 / 대외 한 문장**: **"근거가 증명되는 매수 타이밍."** — 신뢰·데이터·차분함.
- **브랜드 컬러 = 로열블루 `#2563eb`** (`--accent`). 로고(막대+상승선 차트)와 통일. ⚠️ 오션블루 `#0ea5e9`·청록은 폐기.
- **로고 그라디언트**: `#1e3a8a → #2563eb`. 워드마크 `Signal APT`. 부제·로그인 카피도 동일 한 문장.

## 1.5 보이스 & 톤 (신규 UI·카피 작성 시 필수 준수)
- **정체성 한 줄**: "데이터로 증명하는, 내 집 마련의 나침반" — 가치 3기둥 = 근거·맥락·동반.
- **타깃**: 1~2년 내 실거주 매수 수요자(생애 최대 금액 결정).
- **톤 3형용사**: **신뢰 · 명료 · 차분한 자신감** — 귀엽거나 요란하지 않다.
- **보이스 Do**: 근거 먼저·수치로 / 명사형 단정 라벨 / 담백한 존댓말 / 판단은 확률·조건부.
- **보이스 Don't**: 느낌표·과장("지금이 기회!") / 헤더 구어체("어때?","살만한 곳") / 이모티콘 인사("👋") / 확신형 예측.

### 이모지 규칙 (남용 차단 — 화면당 장식 이모지 0)
- **허용(기능 아이콘 화이트리스트)만**: `✕`닫기 · `★ ☆`즐겨찾기 · `✓`완료 · `📍`지도핀 · `🔔`알림. 그 외 UI 이모지 금지.
- **헤더·섹션 타이틀·탭·버튼은 텍스트만**(필요 시 얇은 라인 아이콘).
- **마스코트 예외 `🦊`**: Nick 자기 표면(FAB·챗봇 헤더·인사)에만. 문맥 진입 버튼은 텍스트 "Nick에게 물어보기".

### 용어 사전 (기존 → 권장)
- 지금 살만한 곳 → **내 예산 추천 지역** · 동네 딥다이브 → **동네 리포트** · "안녕하세요 ○○님 👋" → **"○○님, 오늘의 시장 요약입니다"**.
- 유지(전문 신뢰 언어): 시그널 · 급지 · 전세수급 · 매수우위 · 저평가 · 기회도.

### 색 위계 (Color Hierarchy)
- **뉴트럴 지배(≈90%)**: 잉크 `--txt`·그레이·여백이 기본. 프리미엄감은 여백·타이포에서.
- **브랜드 블루 `--accent` = 행동에만(≈10%)**: 버튼·링크·탭 활성. 카드 테두리·배경 남색 남용 금지(강조는 `border-left` 또는 헤딩 색으로 절제).
- **시그널색(초록/노랑/빨강) = 의미 전용**. 브랜드 블루가 이 위계를 침범하지 않는다.

### 마스코트 Nick (유지하되 절제)
- 포지션 "친근한 전문가": 쉽게 설명하되 근거로 말한다. 존댓말·담백, 과한 감탄사·이모지 금지.

## 2. 토큰 (`:root`, 단일 소스)
```
표면   --bg #f8fafc · --panel #fff · --line #e2e8f0 · --txt #1e293b · --dim #64748b
브랜드 --accent #2563eb · --accent-ink #fff · --accent-weak rgba(37,99,235,.10) · --ring rgba(37,99,235,.22)
시그널 --sig-strong #22c55e · --sig-buy #16a34a · --sig-watch #d97706 · --sig-neutral #94a3b8 · --sig-sell #dc2626
의미   --quick #ea580c(급매) · --success #16a34a · --danger #dc2626 · --warn #d97706
형태   --r-sm 8 · --r-md 12 · --r-lg 16 · --shadow 0 8px 24px rgba(2,32,71,.09)
```
- **색은 반드시 토큰 사용.** hex 하드코딩 금지 (특히 브랜드색 = `var(--accent)`).
- **시그널 색은 3곳이 동일해야 함**: CSS `.STRONG_BUY/.BUY/...`, JS `SIGCOLOR`, `_SIGC`. STRONG_BUY=`#22c55e`, BUY=`#16a34a`, WATCH=`#d97706`, NEUTRAL=`#94a3b8`, SELL_RISK=`#dc2626`.

## 3. 타이포 스케일 (px)
| 용도 | 크기 |
|---|---|
| 보조/캡션 | 11 (`--dim`) |
| 기본 소형(칩·표) | 12 |
| 본문 | 13 |
| 카드 제목·라벨 | 14 |
| 섹션/버튼 강조 | 15~16 |
| 페이지 h2 | 19~22 |
| 히어로 수치 | 24~34 |
- 신규 텍스트는 이 단계에서 고를 것. 12.5/13.5 같은 반값은 피함.

## 4. 컴포넌트 규칙
- **버튼**
  - Primary: `background:var(--accent); color:#fff` — **accent 배경엔 항상 흰 글씨.** 패딩 `7~13px × 11~14px`.
  - Ghost/보조: `.btn`(패널 배경 + `--line` 테두리). 위험: `color:var(--danger)`.
  - 차트바 등 눈에 띄어야 할 칩: `.chartbar .chip`(13px).
- **칩/pill**: `.chip`(11px), 활성 `.chip.on`(accent 배경+흰글씨). `.pill` 정보 태그.
- **카드**: `1px solid var(--line)` + `border-radius:var(--r-lg)` + hover `--shadow`. 예: `.ncard`(뉴스), `.gcard`(용어).
- **모달(dialog)**: `border-radius:16px(--r-lg)`, 헤더 = 제목(좌) + ✕(우), `max-width:9x vw`. 상세 모달 우상단에 액션(예: ＋비교담기).
- **리스트 그리드**: 카드형은 `repeat(auto-fill,minmax(260~300px,1fr))`로 반응형.
- **AI 결과**: `mdLite()`로 렌더(빈 줄 제거·타이트). 하늘색 박스 `#eff6ff/#bfdbfe`.
- **권역 공통 라벨**: `.gwon`(앰버). **폴백**: 키/데이터 없으면 조용히 숨기거나 안내(토스트/점선 박스).

## 5. 라운드·간격
- 라운드: 작은 요소 8(`--r-sm`), 카드/입력 12(`--r-md`), 모달/큰 카드 16(`--r-lg`). 무분별한 5/9/11/20 지양.
- 간격: 4·8·12·16·20 기준.
- 포커스: 입력 focus 시 `box-shadow:0 0 0 3px var(--ring)`.

## 6. 내비게이션
- 상단 4탭 = **홈** · **시장·지역**(시그널·저평가) · **매물** · **내 집찾기**(시그널맵·AI 리포트).
- **매물 2단 IA**: 기본 = 전체·급매·청약 / 더보기 = 경매·재건축·갈아타기 (실거주 기본 경로 보호).
- 우측 유틸: 뉴스 · 가이드 · 🔔 알림 · 계정(프로필·관리자).
- 경기 사이클은 별도 탭이 아니라 **시그널 차트 오버레이 + 가이드 모달**로 통합.
- **기본 여정**: 홈(예산 aha) → 시장·지역(BUY+) → 동네 리포트 → ★관심 → 매물 전체(지역 필터) → 주간 변화(피드·이메일).
- **새 기능은 기존 그룹에 편입**하거나 유틸 칩으로. 상단 탭 무한 증식 금지.

## 7. 신규 기능 추가 체크리스트
1. 색: `var(--accent)`·시그널 토큰만 사용했나? accent 배경에 흰 글씨인가?
2. 폰트/라운드: 스케일(§3·§5) 안에서 골랐나?
3. 카드/모달/버튼: 기존 클래스(`.ncard/.gcard/.chip/.btn`)·패턴 재사용했나?
4. 키·데이터 없을 때 graceful 폴백이 있나?
5. 탭을 새로 늘리기 전에 기존 그룹/유틸 칩으로 되는지 검토했나?
